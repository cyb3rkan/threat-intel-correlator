# deploy/k8s/serviceaccount.yaml
# Minimal service account — no RBAC roles attached.
# automountServiceAccountToken: false prevents credential injection.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tic-api
  namespace: tic
  labels:
    app.kubernetes.io/name: tic
automountServiceAccountToken: false
